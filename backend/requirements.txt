# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
starlette>=0.40
pydantic>=2.9

# HTTP / parsing
httpx>=0.27
beautifulsoup4>=4.12
python-multipart>=0.0.9
python-dotenv>=1.0

# Document extraction
pypdf>=4.0
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1

# OCR / images
pytesseract>=0.3.10
Pillow>=10.0
PyMuPDF>=1.24   # PDF page rasterization for OCR fallback (no system deps required)

# AI provider (optional at runtime - controlled by OPENAI_ENABLED / OPENAI_API_KEY)
openai>=1.40

groq>=1.7
